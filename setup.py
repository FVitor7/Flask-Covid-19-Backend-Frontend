from setuptools import setup

setup(
   name='corona',
   version='1.0',
   description='A useful module',
   author='fabvitor',
   author_email='fabvitor2010@gmail.com',
   packages=['corona'],  #same as name
   install_requires=[], #external packages as dependencies
)
